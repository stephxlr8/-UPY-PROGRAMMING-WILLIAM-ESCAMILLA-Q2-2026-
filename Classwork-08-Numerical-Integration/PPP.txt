#Integration
#INPUT

GET a, b, f_x from user
n <- 1000

#PROCESS

IF method == "RRM"
    shift = 1
h <- (b - a) / n

ELIF method == "MRM"
    constant <- h / 2
FOR xi IN RANGE (0 + shift, n + shift) DO

    #Rectangle height
    height <- f_x * xi
    
    area <- area + height * h
END FOR

#OUTPUT

DISPLAY "The integration of  " + f_x + " is " + area
